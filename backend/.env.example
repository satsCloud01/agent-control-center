# Agent Control Center — Environment Variables
# NOTE: API keys are NOT loaded from env. They are provided via the UI
# and passed in request headers. This file is for reference only.

# No secrets needed in .env — all API keys come from the browser UI
